# SceneWeaver 实施进度

## 2026-07-28：自定义片段选片多格式导出桌面 E2E

- 真实桌面 E2E 现从自定义集合中的片段选片卡片保存 1.5–2.5 秒推荐入/出点，验证 SQLite 将其持久化并优先覆盖原镜头范围。
- 测试实际点击 CSV、JSON、EDL 与 FCPXML 导出按钮并读取写出的文件：CSV 保留时间码，JSON 以公开的最终 `start_ms/end_ms` 字段表示范围，EDL 写出 30fps 非丢帧源范围，FCPXML 写出对应 `asset-clip` 起点和时长。

## 2026-07-28：Entity 正/负参考图桌面 E2E

- 真实桌面 E2E 现会创建隔离 Entity，并从实体页实际添加红色正参考图及蓝色负参考图；界面计数与 SQLite 参考记录均验证为一正一负。
- 测试随后在同一实体卡片点击“查找实体素材”，确认结果区域实际渲染，并验证正参考命中的红色素材排在负参考蓝色素材之前；该流程不依赖 API Key、模型或网络。
- 为保证自动化不会控制系统文件选择器，路径桥接仅在开发构建可用；production 始终以用户明确选择的文件作为参考图。

## 2026-07-27：参考图相似检索桌面 E2E

- 真实桌面 E2E 在隔离素材库中扫描两张颜色不同的 PNG，再从搜索页实际点击“参考图”，以独特颜色的本地参考图召回 `reference-fixture.png`；该流程不需要 API Key、模型或网络。
- 自动化无法可靠操作系统文件选择器时，仅开发构建会读取临时 E2E 路径桥接；production 构建保持用户明确选择文件的系统对话框，参考图不会被上传、复制或保存。

## 2026-07-27：Entity 别名搜索桌面 E2E

- 真实桌面 E2E 现从 Entities 页面创建隔离实体和 `scan-fixture` 别名，再在搜索页选择该实体完成本地召回；实际校验素材文件名与“本地实体匹配（名称、别名或正参考图）”解释。
- 为实体导航补充稳定的 `data-testid`，使自动化直接操作真实导航而不依赖 emoji/本地化文案。

## 2026-07-27：崩溃恢复扫描桌面 E2E

- 桌面 E2E 新增 1,000 个隔离 PNG 的真实恢复场景：页面启动扫描任务后强制结束 debug 应用，再以相同的 `SCENEWEAVER_E2E_DATA_DIR` 重新启动，确认启动恢复队列自动继续任务并全部入库。
- 测试进程把应用启动、WebDriver 连接和强制结束后的重连封装为可重复步骤；测试结束始终清理专用临时应用数据目录，不读取用户 SQLite 或素材。

## 2026-07-27：无 API Key 本地搜索桌面 E2E

- 真实桌面 E2E 现从刚扫描的隔离素材中输入 `scan-fixture` 并点击搜索，验证本地关键词结果数量、文件名和“满足必须条件”解释；该路径不依赖 CLIP 模型、API Key 或网络。
- 暂停/恢复步骤改为等待同一真实任务按钮完成状态切换后再检查 SQLite，消除 React 事件与查询刷新之间的自动化竞态；本机完整 E2E 已实际通过。

## 2026-07-26：超长路径缩略图与桌面扫描 E2E

- 真实桌面 E2E 新增中文/空格、超过 260 字符路径的有效 PNG 和损坏 PNG：测试通过页面真实扫描按钮启动任务，确认长路径素材入库并返回 JPEG 缩略图，损坏图片仅缺失缩略图而不会阻断任务或隐藏有效素材。
- 修复 Windows 超长路径缩略图：图片缩略图服务改用 Rust 文件 API 读取字节后在内存中解码，避免部分图像后端直接打开长路径时的 `MAX_PATH` 兼容问题；`core_smoke` 同时断言长路径 JPEG 缩略图实际写出。
- E2E 的详情页资产断言改为等待真实异步渲染，避免扫描完成后 React Query 尚在加载时产生假失败；本机已实际通过完整桌面 E2E。

## 2026-07-26：Windows 发布图标与安装器复验

- 用可审计的 `src-tauri/icons/sceneweaver.svg` 生成多分辨率 Windows `icon.ico`，替换旧的 70-byte 占位图标；Tauri 配置已显式使用该 ICO。
- 带新图标的当前 NSIS 安装器已完成隔离静默安装与 8 秒启动 smoke：退出码 0，EXE（58,777,785 bytes）、`WebView2Loader.dll` 与 `onnxruntime.dll` 均同级；专用测试目录已清理。

## 2026-07-26：当前 NSIS 安装包复验

- 已用当前代码重新生成 `SceneWeaver_0.1.0_x64-setup.exe`，并静默安装到专用临时目录：安装器退出码为 0，`sceneweaver.exe`（58,726,850 bytes）、`WebView2Loader.dll`（160,320 bytes）和 `onnxruntime.dll`（14,897,976 bytes）均同级。
- 已从安装目录启动 EXE 并确认其隐藏存活超过 8 秒，然后终止进程并清理专用测试目录；该流程没有下载或使用 Edge。

## 2026-07-26：可选真实视频扫描桌面 E2E

- 向 `npm.cmd run test:e2e` 明确传入 `SCENEWEAVER_E2E_FFMPEG_BIN` 后，测试会使用该本机可执行文件生成隔离 MP4，扫描并确认视频时长与镜头片段均已写入 SQLite；本机实测已通过。
- 默认 E2E 不下载、安装或猜测 FFmpeg，变量未设置时只跳过这项可选视频检查并继续验证无 FFmpeg 的核心桌面流程。

## 2026-07-26：隔离扫描暂停/恢复桌面 E2E

- 桌面 E2E 现建立 200 个隔离 PNG，点击真实扫描按钮后进入任务中心，依次点击暂停、恢复，并以 SQLite 的任务状态和最终 200 个已入库资产验证队列控制闭环。
- 第二次 API 建库后素材库页可能暂存旧 React Query 数据；测试会刷新真实窗口后再定位新卡片，避免把前端缓存时序误判为后端扫描问题。

## 2026-07-26：隔离真实素材库扫描 E2E

- E2E 会在其独立临时数据目录创建一个有效 PNG 文件，以真实 `create_library` 命令创建素材库后点击页面的“扫描”按钮；测试等待真实后台任务完成，并在素材库详情页确认资产已入库。
- 这补足了桌面自动化中的真实图片扫描入口；目录选择系统对话框、真实用户视频与扫描暂停/恢复仍另列为待验证项。

## 2026-07-26：隔离桌面片段选片 CSV 导出 E2E

- `npm.cmd run test:e2e` 现会在真实桌面窗口中从自定义片段集合点击 CSV 导出，并读取隔离临时目录中的实际文件，确认文件名和片段入点 `00:00:01.000` 均被写出。
- 为避免测试时打开系统保存对话框，开发构建仅在测试显式设置且文件扩展名匹配时读取临时输出路径；production 构建已验证不包含该变量，正常用户导出仍始终使用系统保存对话框。

## 2026-07-26：隔离桌面片段选片 E2E

- `webdriver-e2e` feature 下的测试夹具会创建隔离 SQLite 素材库、视频资产、镜头片段和自定义集合；真实窗口已验证素材库→镜头面板→选择自定义集合→加入片段→Selects 回读整个路径。
- 修复测试数据目录：Tauri 不会自行解释通用环境变量，现仅由测试 feature 读取 `SCENEWEAVER_E2E_DATA_DIR`。测试脚本为每次运行创建并删除该目录；正常 debug、release 与 NSIS 不会读取该变量，也不会编译测试夹具。

## 2026-07-26：片段加入自定义选片集合

- 镜头片段面板现在可选择“我的选片”或任一自定义集合；写入仍由后端同时校验集合、资产和片段的归属，重复加入保持幂等。
- `core_smoke` 已实际验证自定义集合保存片段、保存推荐入/出点并写出包含推荐时间码的 CSV；页面 jsdom 测试覆盖选择自定义集合后提交的集合/资产/片段 IPC 参数。

## 2026-07-26：自定义选片集合交互回归

- 新增 Selects 页面 jsdom 交互测试，实际覆盖自定义集合的 CSV 保存路径选择与导出调用、键盘替代“下移”排序写回，以及推荐入/出点从秒转换为 SQLite 毫秒值。
- 后端 `core_smoke` 已覆盖默认与自定义集合的真实 CSV/JSON/EDL/FCPXML 写出与顺序；本轮补足页面到 IPC 参数的回归保护。真实桌面窗口中的素材导入、片段选片和导出仍需继续纳入 E2E。

## 2026-07-26：桌面 E2E 基础

- 新增 `npm.cmd run test:e2e`，真实启动带 `webdriver-e2e` feature 的 debug SceneWeaver、临时本地 Vite 服务和应用内嵌 WebDriver；已实际验证窗口标题、首页壳层渲染、导航至搜索页，以及创建自定义选片集合后刷新仍由隔离 SQLite 保留。
- `test:e2e` 脚本现自行把 `C:\msys64\mingw64\bin` 放入其 Cargo 子进程路径，避免独立运行时因找不到 GNU `ktmw32` 导入库而在链接阶段中断。
- E2E 直接连接应用内 HTTP WebDriver 端口，不使用、下载或管理 Edge Driver / Chrome Driver；每次为测试应用创建并清理独立临时数据目录，避免读写或锁定用户 SQLite；feature 为显式可选项，release 与 NSIS 安装包不会启用测试服务器。
- 本轮重新构建 NSIS 后，已实际静默安装、确认 `WebView2Loader.dll` 与 `onnxruntime.dll` 同级部署并启动超过 8 秒；另实际启动 release EXE，确认应用存活且 `127.0.0.1:4445` 未开放。
- jsdom 仍承担快速页面交互覆盖；素材导入、扫描、片段选片及导出尚待扩展为真实桌面场景。

## 2026-07-26：素材日期来源可解释

- 媒体卡片现显示“拍摄/创建”或“文件修改”及对应本地日期，和日期范围筛选的优先级完全一致。
- 前端单测覆盖可信 capture_time 优先和修改时间回退，避免把文件时间伪称为拍摄时间。

## 2026-07-26：FFprobe 超时回收

- FFprobe 元数据读取改为保留子进程句柄的轮询监督；30 秒超时时会执行终止与 wait 回收，再将该素材安全降级。
- core_smoke 已通过实际 FFprobe 视频元数据、创建时间与派生流程；还会启动本地慢进程，以 100ms 超时断言两秒内返回，直接覆盖终止并回收子进程的路径。该改动消除了旧通道超时路径可能遗留的后台探测进程。

## 2026-07-26：1,000 文件扫描验收

- `core_smoke` 现会创建独立的 1,000 图片素材库，验证首次扫描完整入库、SQLite 资产数准确，以及紧随其后的增量扫描全部识别为未变化文件。
- 压力素材与缓存只位于系统临时目录；测试结束即清理，既不读取也不修改用户媒体。

## 2026-07-26：搜索与任务页交互测试基础

- 已接入 `jsdom`、React Testing Library 与 `user-event`，在 Vitest 中实际渲染搜索页并执行用户输入、点击、编辑和移除操作。
- 新增用例验证结构化查询请求、条件芯片编辑/删除后的重新检索，以及开始日期晚于结束日期时按钮禁用且不发出搜索请求；任务页则验证暂停/恢复命令与 `scan:progress` 状态、步骤、计数更新。Tauri IPC 在该层明确模拟，桌面安装包内的完整 GUI E2E 仍待验证。

## 2026-07-25：实体候选反馈闭环

- 实体页将候选标记为“是实体”或“非实体”后，反馈仍只存于本地 SQLite，同时自动重新运行当前实体检索，使正样本扩展或负样本抑制立即体现在候选排序中。
- 这不是身份置信度声明；它只让用户确认的本地样本真正参与下一次可见结果。

## 2026-07-25：图片 EXIF 拍摄时间

- 新增本地 EXIF 读取：DateTimeOriginal、DateTimeDigitized 或 DateTime 只有在拥有对应 OffsetTime 时才会写入 capture_time；带时区的时间会转换为 Unix 毫秒。
- 不带时区的传统 EXIF 时间、损坏元数据及不支持的容器均回退文件修改时间，不按当前机器时区猜测，避免跨时区照片被错误归档。
- core_smoke 以应用内生成的 JPEG APP1 EXIF 样本实际扫描、生成缩略图并回读 SQLite 的 capture_time；没有读取、上传或保留用户媒体。

## 2026-07-25：视频/音频创建时间提取

- FFprobe 现读取容器或视频流中的标准 RFC 3339 creation_time，以及兼容的 QuickTime 创建时间，并以 Unix 毫秒持久化到现有 capture_time。
- 日期搜索对该字段优先于文件修改时间；无时间、格式不带时区、FFmpeg 缺失或图片素材仍安全回退到文件修改时间，不会猜测日期时区。
- core_smoke 真实生成带 creation_time 的 MP4，验证 FFprobe 读取、扫描写入和 SQLite 回读；Rust 单测也拒绝无时区的模糊日期。

## 2026-07-25：本地日期范围搜索

- 搜索页新增起止日期，范围为包含当天首尾的本地日期；开始晚于结束时会明确阻止提交。
- 关键词和可选 CLIP 语义候选共享 SQLite 的硬日期约束，参考图、相似图和 Entity 候选也会在结果展示前应用相同本地筛选，不会因检索入口不同而忽略用户条件。
- capture_time 初始仅作为优先字段预留；日期范围最初以文件修改时间工作。现已补齐视频/音频容器创建时间，图片 EXIF 仍按文件修改时间兜底；新增前端边界测试及 core_smoke 的 SQLite 包含/排除验证。

## 2026-07-25：复制相对素材路径

- 媒体卡片新增“相对路径”操作，后端从资产 ID 和所属素材库根目录计算后写入剪贴板，方便交给剪辑工程或协作者。
- 路径不属于素材库根目录、素材或库记录缺失时会拒绝复制，不使用页面传入路径。

## 2026-07-25：实体结果来源解释

- 通过实体下拉框检索时，每个候选素材都会显示“名称、别名或正参考图”的本地匹配来源，避免 Entity 结果成为无解释黑箱。
- 当前展示的是候选来源说明，不伪装为跨 Provider 校准的身份置信度；负参考仍以保守排序惩罚参与后端候选生成。

## 2026-07-25：选片批量打开

- Selects 的已选项目可一键批量打开源素材；片段项与素材项会先按资产 ID 去重，避免同一文件被重复打开。
- 前端不会提交文件路径，继续复用后端按已授权资产 ID 解析并打开源文件的安全边界。

## 2026-07-25：首次导入三步引导

- 首页在尚无素材库时显示选择目录、索引模式与开始后台扫描的三步引导；成功后刷新素材库、任务与统计状态。
- 引导完全复用已有目录安全校验、素材库创建和持久化扫描队列，不复制、移动或上传用户媒体。

## 2026-07-25：本地隐藏搜索反馈

- 搜索卡片可将不想再看到的素材标记为隐藏；关键词、相似图、参考图与实体结果会在界面层统一排除这些项，并可一键恢复全部。
- 隐藏状态持久化于 SQLite `hidden_assets`，使用 v9 迁移和资产外键级联；不会修改源媒体、收藏、选片或分析数据。
- `core_smoke` 已验证隐藏写入幂等、重开数据库可读取及恢复后的清空。

## 2026-07-25：选片标签筛选与批量编辑

- Selects 可按标签筛选，支持全选当前筛选结果并批量添加本地标签；筛选不改变排序、推荐时间范围或源素材。
- 批量写入在单个 SQLite 事务中执行：重复标签幂等跳过，任一选片项已删除则整批失败，不会留下半批标注。
- `core_smoke` 已验证多项批量添加、重复批量去重与既有导出路径。

## 2026-07-25：选片标签与审片清单元数据

- Selects 的每一项现在可添加、移除多个本地标签；标签属于选片项而非源素材，不会更改文件或自动推断画面内容。
- 标签写入已有 SQLite 通用标签表，支持大小写无关去重；删除选片项、删除素材库或重新切分视频时会同步清理对应标签，避免遗留无归属记录。
- CSV、JSON 和离线 HTML 联系表均会显示标签。`core_smoke` 已验证添加、重复去重、导出及重新切分后的清理。

## 2026-07-25：搜索结果批量选片

- 搜索、相似图与实体结果卡片支持多选、全选当前结果，并可一次性幂等加入默认或任意自定义选片集合；切换搜索来源或成功写入后会清空选择。
- 后端批量写入使用单一 SQLite 事务，重复提交同一素材不会产生重复选片项；目标集合已删除时会明确拒绝写入。
- `core_smoke` 验证默认与自定义集合的批量写入、重复提交去重。

## 2026-07-19：数据库快照导出

- 设置页可导出一致的 SQLite `.db` 快照；使用 `VACUUM INTO`，拒绝覆盖已有文件，不包含原始媒体或缓存。
- `core_smoke` 已验证备份可重新打开、保留库记录且不能覆盖现有快照。

## 2026-07-19：真实缓存管理

- 设置页现显示并可清理本地媒体缓存；仅清除缩略图、关键帧、短预览和波形，不清除模型、SQLite、选片记录或源素材。
- `core_smoke` 验证缓存字节统计、清理与模型缓存保留。

## 2026-07-19：素材库重新连接与移动恢复

- 新增本地“重新连接”工作流：安全校验新目录后，按原根目录下的相对路径重绑已有资产，保留稳定资产 ID、缩略图、向量、镜头和选片关联；不再把移动后的文件误建为重复资产。
- 重连会立即创建增量扫描任务；未在新根目录找到的旧资产会标为 `offline`，扫描中的素材库拒绝重连以避免并发写入。
- `core_smoke` 已实际覆盖目录移动、嵌套相对路径、保留 Selects/缩略图/资产 ID、缺失文件离线以及重连后无变化跳过重处理。源媒体文件不会被应用修改。

## 2026-07-19：同库扫描任务去重

- SQLite 现在会查找同一素材库处于 `pending`、`running` 或 `paused` 的扫描任务；新扫描和重连自动创建扫描前都会拒绝重复任务。
- 素材库页面轮询任务状态并禁用已排队/运行/暂停库的扫描和重连按钮；后端仍是最终约束，并会提示用户恢复或取消暂停任务。
- `core_smoke` 覆盖运行中任务与崩溃恢复后 pending/人工暂停任务均会被识别为活跃扫描，防止重复入队。

## 2026-07-19：离线 HTML 联系表

- 默认和自定义 Selects 均可导出单文件 HTML 联系表；卡片按当前排序输出，并展示时间范围、类型、绝对路径、评分和备注。
- 可用的素材/片段缓存缩略图会嵌入为 data URI，导出文件不依赖 SceneWeaver 缓存路径即可离线审阅或打印；缺失缩略图会明确显示占位说明。
- `core_smoke` 已验证实际 HTML 写出、嵌入 JPEG 缩略图和推荐/片段起点时间码。

> 本文档实时记录各阶段完成情况、测试结果与下一轮任务。
> 所有记录基于 `SceneWeaver_PRODUCT_REDESIGN.md`（V2 规范）。

## 当前阶段

阶段 1：本地素材库与增量扫描（纵向闭环实现中；质量门禁已开始建立）

阶段 2：已实现扫描时自动执行的 FFmpeg 场景边界切分、关键帧、短预览、基础黑帧/模糊检测与片段持久化；片段可展示质量指标、按需播放短预览并加入 Selects。搜索结果的视频卡片会显示已定位片段数；打开片段面板时只展示本次结构化条件命中的镜头，完成搜索到精确片段、时间码和片段级选片的闭环。关键帧的下部高对比度文字形态会以保守启发式写入 `subtitle_present`；“字幕 / subtitle”“黑帧 / 黑屏”“模糊 / blur”现均可作为视频素材的 Must / Should / Must Not 条件。黑帧比例达到 85%、模糊分达到 80% 才会产生对应提示；它们和字幕提示都属于代表帧启发式，不等同 OCR 或真实画质评测。结构化 Must/Must Not 会在同一片段上组合：包含多个不同镜头的视频不会因其中一个不合格镜头而被整条排除，但“字幕且不要模糊”等组合也不会由两个不同镜头错误拼接满足。FFmpeg 缺失、镜头检测失败或单个派生文件失败均不会中断素材入库，视频仍可搜索并可稍后手动重试切分。镜头检测与派生各自的 90 秒超时会终止并回收 FFmpeg 子进程，避免遗留后台进程。2026-07-19 已用本机 FFmpeg 8.1.2 合成红蓝切换 MP4 实测自动扫描路径，验证片段、关键帧、短预览、黑帧/模糊/质量指标和字幕字段均写入 SQLite 与缓存；smoke 同时验证字幕、黑帧和模糊条件的 Must 命中、Must Not 安全片段保留及同镜头组合约束。

阶段 3：已实现本地持久化图片视觉特征与相似图片召回，并接入可选的本地 CLIP ViT-B/32 图文 Provider。设置页只有在用户点击后才下载模型并允许重建语义索引；运行库随安装器携带，模型缺失、运行库缺失或推理失败均降级为关键词/颜色检索。CLIP 模型的中文通用语义质量尚未验证，不将其宣称为中文模型。

为提高常见中文创作搜索的可用性，已加入透明的本地中英提示词典：角色、雨夜、侧脸、回头、战斗、字幕、近/远景等已知词会扩展为 CLIP 英文提示；未知中文专有名词保持关键词降级。此机制不替代通用中文模型。

阶段 3 索引基础：`asset_embeddings` 已迁移为 `(asset_id, provider_id)` 复合主键；基础颜色特征与后续语义模型能够同时持久化，检索按 Provider 隔离，不会因模型切换覆盖旧索引。

`core_smoke` 已用合成 v6 SQLite 数据库验证上述迁移会保留既有向量，并允许同一素材写入第二个 Provider。

本轮还修复了 Entity 参考向量在多 Provider 数据库中未限定 Provider 的问题：实体颜色参考和资产颜色索引现在明确使用 `local-color-histogram`，不会把 24 维颜色向量与 CLIP 向量混合排名；smoke 同时验证新建数据库初始即为 v8，重开不会重放旧迁移。

本轮已从 ONNX Runtime 官方 Windows x64 CPU 发布包随应用加入 `onnxruntime.dll`；`core_smoke` 验证运行库存在且不会隐式下载模型。NSIS 资源配置会将其与 EXE 同级携带。

`semantic_model_smoke` 已在独立临时目录真实下载配对 CLIP 图像/文本编码器，完成 512 维图像与文本推理、SQLite 语义文本召回、实体语义参考召回并清理临时模型；这验证了动态 ONNX Runtime、模型缓存布局、Provider 的实际推理路径以及按 Provider 持久化检索。它仍不是中文语义效果评估。

阶段 4：已实现 Must / Should / Must Not 本地条件过滤、Should 软排序、结果解释、搜索历史，以及可直接编辑或移除的条件芯片。ACG 标签命中会在结果解释与 Should 完成状态中正确反映；关键词、CLIP 和 Entity 多路候选合并后会按可解释分数、更新时间和稳定 ID 排序。同分候选再按素材库稳定轮换，避免单个库挤占前排，并在结果原因中说明。视觉/元数据条件和跨 Provider 分数校准尚未完成。

语义 Provider 已安装时，搜索会将关键词与 CLIP 图文候选去重合并；Entity 候选也会进入同一排序。媒体类型、片段质量与 Must Not 仍由 SQLite 作为硬约束。结果解释会区分 CLIP 语义向量、颜色特征与 ACG 标签；同分候选按素材库轮换，当前尚未做跨路分数校准。

阶段 6：选片集合、移除/归档、标签、评分、备注、推荐入/出点、拖拽排序，以及默认/自定义集合的 CSV/JSON/EDL/FCPXML/PNG/HTML 联系表导出已实现；片段范围与推荐入/出点会写入各时间线格式。剪辑软件导入验证尚未完成。

阶段 5：已实现通用 Entity 创建、名称/别名匹配、正/负本地参考图召回及误加参考图移除。实体参考向量现可按 Provider 持久化：颜色参考始终走颜色索引，安装并重建 CLIP 后参考图与素材反馈也会写入语义 Provider 并参与合并召回。视频代表帧会以“双下角同时出现小面积高对比元素”的严格启发式写入 `game_ui`；`UI` / `HUD` 可作为片段级 Must / Should / Must Not 条件，但它不是通用 UI 或菜单识别。实体置信度、真实中文语义效果评估与完整 ACG Pack 尚未完成。

实体现在既可从主搜索页直接选择，也可在自然语言请求中通过名称或别名自动识别并召回；名称/别名命中、颜色参考和（已重建时）语义参考会去重合并，卡片会显示命中的实体名称。仅出现在 Must Not 的实体不会被用作正向召回。无语义模型时仍可使用名称/别名和颜色参考，不会阻塞核心搜索。

ACG Creator Pack：已支持用户为素材添加本地确认标签，标签会持久化并参与 Must / Should / Must Not 组合查询；保守 HUD 提示可辅助简短 `UI` / `HUD` 片段筛选，中文“游戏 UI”仍保留给用户确认标签。自动战斗、视角与表情识别及通用 UI 识别仍待视觉模型能力完成。

ACG 查询预设已接入搜索页（角色近景、雨夜侧脸、战斗无 UI/字幕、剧情过场、情绪结尾）；预设先写入可编辑条件芯片。ACG 同义词对 Must Not 也作为硬排除写入 SQLite，而非错误地作为 Should 加分。

导出：FCPXML 的 Windows 源路径现以 UTF-8 百分号编码为 `file:///` URI，并依据素材类型输出音频或视频属性，减少中文与保留字符路径的导入歧义。每个格式和素材资源使用独立 ID；时间线采用首个有效帧率，视频素材资源保留已探测的帧率与分辨率（包括 NTSC 分数帧时长）。真实剪辑软件导入仍待验证。

## 已完成功能

- 完整阅读 V2 产品规范。
- 环境审计与部署：
  - Node.js 已可用。
  - 部署 Rust（GNU 工具链）与 MinGW-w64 编译器，避免依赖 Visual Studio。
  - 安装 Tauri CLI v2。
- 创建项目核心文档：
  - `README.md`
  - `docs/ARCHITECTURE.md`
  - `docs/DATABASE.md`
  - `docs/SEARCH_PIPELINE.md`
  - `docs/TEST_PLAN.md`
  - `docs/PACKAGING.md`
  - `docs/DECISIONS.md`
  - `docs/KNOWN_LIMITATIONS.md`
- 创建 Tauri v2 + React + TypeScript + Vite 桌面应用骨架。
- 前端页面：
  - 首页（统计卡片、导航）
  - 素材库列表与添加
  - 素材库详情与媒体网格
  - 任务中心
  - 设置
- Rust 后端核心模块：
  - SQLite 数据库与迁移（libraries、assets、jobs、segments、tags、entities 等表）。
  - 素材库 CRUD。
  - 目录扫描与格式过滤。
  - 增量文件指纹（基于路径、大小、修改时间）。
  - ffprobe 元数据解析（视频/音频时长、分辨率、帧率、编码）。
  - 图片缩略图（image crate）与视频中间帧缩略图（FFmpeg，若可用）。
  - 持久化任务队列与状态机（pending / running / paused / completed / failed / cancelled）。
  - 扫描任务暂停、恢复、取消。
  - 崩溃恢复：启动时恢复未完成任务。
  - 缓存管理（缩略图、代理、关键帧目录）。
- 前端与后端 IPC 命令封装。
- 2026-07-18 本轮：
  - 添加 ESLint 配置与 Vitest 测试基线。
  - 资产唯一键调整为 `(library_id, normalized_path)`，避免同一文件加入第二个素材库时串库。
  - 扫描改为双遍流式遍历，并以 `last_seen_scan_at` 持久化扫描发现状态。
  - 扫描进度与当前步骤回写 SQLite；任务页轮询可读取持久化进度。
  - 任务中心订阅实时扫描事件，显示进度百分比，并支持失败/取消任务一键重试。
  - 扫描检查点现在持久化已处理数量、总数量和文件级错误数；重启后任务中心可读取这些进度信息。
  - 创建素材库时验证绝对且存在的目录；打开文件/所在文件夹改为资产 ID 后端解析。
  - 资产列表现在返回本地缓存缩略图 data URL，媒体网格显示真实图片和视频封面（后者需要 FFmpeg）。
  - 新增镜头片段 `Segment` 模型、SQLite 查询和前端 API 类型，为镜头检测、时间定位与预览建立持久化接口。
  - 视频卡片可触发 FFmpeg `scene` 场景检测，解析时间边界并写入镜头级片段；FFmpeg 缺失时显式降级为完整视频片段。
  - 片段切分后会按镜头生成代表关键帧、最长 6 秒静音短预览，并从关键帧计算黑帧比例、模糊程度和基础质量分（均在本地缓存内）。
  - 素材库片段面板会显示关键帧、入/出点、质量指标，并可按需载入短预览；关键帧和预览只能从应用缓存读取。
  - 镜头片段可幂等加入“我的选片”；选片卡片显示对应镜头时间范围。重新切分同一视频时会原子清理失效的片段选片引用，避免保留错误时间范围。
  - 素材库详情可从视频卡片打开持久化镜头片段，查看精确入/出时间码并复制入点时间码。
  - 新增搜索页面和本地 IPC，可按已索引素材的文件名、路径进行关键词召回并在结果网格继续执行素材操作。
  - 搜索页解析“不要 / 不含 / 排除”中文表达，Must 词与 Must Not 词实际参与 SQLite 条件查询。
  - 新增 SQLite 收藏表与媒体卡片一键收藏，可跨素材库和搜索结果持久化显示。
  - 媒体卡片可幂等加入“我的选片”，使用现有 selects 数据表持久化，并在独立选片页面查看。
  - “我的选片”页面可由用户选择保存位置，导出 CSV（文件名、路径、媒体类型、时长、入/出时间码）。
  - 选片页面新增 JSON 导出，输出同一批素材的结构化本地路径、类型、时长和时间码。
  - 选片页面新增 CMX 3600 EDL 导出，按选片顺序生成非丢帧 30fps 视频事件。
  - 选片页面新增 FCPXML 1.10 导出；素材格式资源保留已探测帧率与分辨率，顺序时间线使用首个有效素材帧率。
  - 新增实体页面，可本地创建角色、人物、产品和自定义实体，并保存别名。
  - 选片页已接入自定义集合创建、跨集合移动、移除、评分、备注和推荐入/出点；所有标注持久化在 SQLite。
  - 新增选片标注参数校验单元测试（评分范围及推荐入/出点区间）。
  - `Should` 条件现会参与 SQLite 文件名/路径软排序；中文 “优先 / 最好 / 尽量” 与多项 “不要 / 排除” 会解析为可执行条件。
  - 搜索结果页的 Must / Should / Must Not 条件芯片可直接编辑，失焦或回车后使用修改后的结构化条件再次搜索；移除芯片继续立即重新搜索。
  - ACG Creator Pack 开关持久化在本地 SQLite；启用后以本地词典为战斗、剧情、过场、UI、Gameplay、正/侧脸等查询词补充文件名/路径优先匹配。
  - 新增并实际运行 `core_smoke`：验证中文/空格路径、图片缩略图、未变化跳过、修改文件重建元数据、删除后的离线标记、默认选片持久化、CSV/JSON/EDL/FCPXML 导出，以及暂停任务检查点的重开 SQLite 读取。
  - 新增本地 `local-color-histogram` 视觉特征 Provider：图片扫描后把特征与 Provider/版本持久化到 SQLite；搜索结果的“相似”操作会按余弦相似度召回已索引图片，无需 API Key 或模型下载。
  - 基础 Provider 支持红、绿、蓝、黄及夜色等中英文颜色词；关键词没有命中时会用同一特征空间做视觉召回，未知自然语言保持关键词降级而不伪造语义结果。
  - 搜索页新增“参考图”入口：经系统选择器授权的本地图片只在内存中提取特征，后端验证绝对路径、文件存在性和受支持图片扩展名后执行相似检索，不上传、不复制也不保存参考图。
  - 视频扫描在 FFmpeg 可生成封面时，会用该代表帧写入同一视觉特征库；FFmpeg 缺失时跳过该增强步骤，不影响扫描、持久化和关键词搜索。
  - 搜索 IPC 现在返回结构化结果：分数、实际匹配原因和未命中的 Should 条件；搜索卡片会显示这些解释，颜色视觉降级也会明确标记为本地视觉特征命中。
  - 搜索页新增图片、视频和音频类型筛选；条件作为 `media_types` 参数在 SQLite 查询层执行，core smoke 覆盖了结构化类型过滤。
  - 搜索页新增最低视频片段质量阈值；SQLite 只保留至少存在一个达到质量分镜头的视频，图片不会因无镜头数据被隐藏。
  - Entity 页面现在可添加正/负本地参考图；正参考图的本地视觉特征持久化在 SQLite，按实体检索会平均正样本特征并召回相似素材。core smoke 已验证引用持久化与实体召回。
  - Entity 排序现在将负参考图相似度以 35% 权重作为惩罚，正样本相似度与负样本惩罚均在本地 SQLite 特征上计算。
  - Entity 检索现在会先以实体名称和别名匹配本地文件名/路径；没有参考图也可工作，有参考图时会合并正/负参考图的本地视觉召回并去重。
  - `core_smoke` 已实际验证视觉索引写入、重开 SQLite 后的相似素材排序，以及既有中文路径、增量扫描和选片导出流程。
  - 自定义选片集合现在支持按当前排序导出 CSV、JSON、CMX 3600 EDL 与 FCPXML 1.10；导出会优先采用用户设置的推荐入/出点，其次采用关联镜头片段范围。`core_smoke` 已验证片段起点写入四种格式。
  - 选片页现支持直接拖拽卡片排序，顺序持久化到 SQLite；上下按钮保留为键盘替代操作，所有导出沿用同一顺序。
  - 默认和自定义选片集合可导出本地 PNG 联系表：优先使用片段关键帧、否则使用素材封面，按当前集合顺序组成网格。`core_smoke` 已验证 PNG 写出与尺寸。
  - 任务恢复现在会通过 SQLite 事务将应用中断时的 `running` 任务转回 `pending`，保留 checkpoint；用户明确暂停的任务会保持 `paused`，直到用户手动恢复。`core_smoke` 已实际验证重开 SQLite 后仅恢复运行中任务、暂停任务不进入队列以及 checkpoint 保留。恢复后重新执行安全的增量扫描，不宣称媒体处理的精确断点续跑。
  - 安装 FFmpeg 后，`core_smoke` 会自动生成红蓝切换测试视频并验证视频元数据、镜头片段、关键帧、短预览、质量分、黑帧分和模糊分；无 FFmpeg 的环境会跳过该可选检查。
  - `core_smoke` 现生成超过 260 字符的中文路径素材并混入损坏 PNG，验证超长路径可索引且单个损坏素材不会阻断其它文件扫描。
  - 新增可选本地 CLIP 图文 Provider、设置页模型状态/明确下载/语义重建操作，以及按 Provider 隔离的语义文本与参考图检索；未安装模型不联网并继续走已有本地检索路径。
- Entity 参考向量新增独立的 Provider 维度（schema v8）；从旧 `embedding_ref` 数据迁移为颜色 Provider。实体的资产“是/非”反馈会同步保存当前素材已有的各 Provider 向量，重新建立语义索引也会处理可访问的实体参考图与素材反馈来源。
 - 增量扫描发现已索引源文件变化时，会原子清理该素材的旧 Provider 向量、镜头片段和片段级 Selects，并清理同一素材的缩略图/关键帧/预览缓存；重新建立基础视觉向量后，会同步保留实体反馈的当前颜色向量，旧语义向量必须经用户明确的语义重建操作恢复。
  - 语义检索现在只把解析后的 Must/Should 条件转换为 CLIP 提示；Must Not 永远不进入提示，避免“不要字幕/UI”反向提高被排除画面的语义相关性。可审计词典扩展到常见场景、景别、视角、动作、情绪和发色词，未知中文仍保持关键词降级。

## 尚未完成功能

- 素材库创建、扫描、片段选片和导出的桌面端到端场景（当前已验证真实窗口启动与首页→搜索导航；release EXE 已验证可启动）。
- 真实素材库扫描验证（超长路径、视频与损坏文件）。中文和空格路径已由 `core_smoke` 覆盖。
- 真实用户视频素材的关键帧、短预览与质量检测验证（合成视频已覆盖）。
- 经真实中文素材集评估和必要替换后的中文自然语言视觉语义模型，以及更强的视频多帧视觉索引。
- 基于视觉/元数据的 Must / Must Not 与多路融合排序。
- Entity 置信度、通用语义实体匹配与完整 ACG Creator Pack。
- 专业剪辑软件导入验证。
- MSI、交互式安装/卸载/升级验证。

## 测试结果

- `rustc 1.97.1` 可用（x86_64-pc-windows-gnu）。
- `cargo-tauri.exe` 已安装成功。
- 前端 `npm run build` 成功。
- Rust `cargo build` 成功（通过 `--exclude-all-symbols` 解决 GNU 工具链 DLL 导出符号过多问题）。
- 前端：`npm.cmd run lint` 通过；`npm.cmd test` 通过（17 tests，含 4 项 jsdom 搜索与任务页交互测试）；`npm.cmd run build` 通过。
- Rust：新增路径、指纹、帧率、任务控制、导出、选片标注、视频派生参数单元测试；本轮在补齐 MSYS2 MinGW-w64 的 `windres` 与 `gcc` 后，`cargo test --no-run` 已成功编译；测试二进制运行仍受 GNU/Tauri Windows `STATUS_ENTRYPOINT_NOT_FOUND` 阻塞。
- 已有前端 jsdom 交互测试；2026-07-26 已新增并实际运行真实 Tauri 窗口 E2E，覆盖启动、首页→搜索导航、真实素材库扫描、中文/空格超长路径缩略图、损坏 PNG 容错、任务暂停/恢复、片段选片与 CSV 导出。
- 早先 `tauri-driver` 安装失败是调用 shell 未带 `C:\msys64\mingw64\bin` 的 PATH 问题，而非缺少库；驱动随后可安装。但该外部 driver service 会自动管理 Edge Driver，不符合本项目 E2E 的无浏览器 driver 约束，现改为应用内嵌 WebDriver。
- 已新增 GitHub Actions Windows 质量门禁：在 `main` 推送、PR 和手动触发时执行前端 lint/test/build、Rust 格式/测试编译和不下载模型的 `core_smoke`。首次 MSVC 远端运行已实际发现干净工作目录缺少 `target/release/WebView2Loader.dll` 的构建资源缺陷；现由 `build.rs` 从锁定的 `webview2-com-sys` 依赖暂存 DLL，2026-07-19 的干净 Windows 复验已成功通过 Rust 编译与 core smoke。

## 构建结果

- `npm.cmd run lint`：成功。
- `npm.cmd test`：成功（8/8）。
- `npm.cmd run build`：成功。
- Rust `cargo test --no-run`：成功。
- Rust `cargo build`：成功（已生成桌面可执行文件）。
- Rust `cargo build --release`：成功；当前生成的 `sceneweaver.exe` 已实际启动并在 8 秒进程存活检查后停止。
- Rust `cargo run --bin core_smoke`：成功；实际验证中文/空格路径、缩略图、增量扫描、删除离线、默认选片导出与片段 Selects。
- `npm.cmd run tauri:build -- --bundles nsis`：成功；当前代码已生成 NSIS 安装器 `SceneWeaver_0.1.0_x64-setup.exe`。
- NSIS 安装 smoke：2026-07-26 当前安装器已成功在专用临时目录静默安装（退出码 0），确认 EXE（58,726,850 bytes）、`WebView2Loader.dll`（160,320 bytes）、`onnxruntime.dll`（14,897,976 bytes）均同级且应用隐藏运行超过 8 秒；临时安装目录已清理，且该检查没有下载或使用 Edge。
- Windows release EXE 启动验证：成功（进程运行超过 8 秒后由验证脚本停止）。
- Rust `cargo test`：测试二进制启动失败（`STATUS_ENTRYPOINT_NOT_FOUND`），待迁移 MSVC 工具链或修复 GNU 运行时加载问题。
- `tauri dev`：未完整验证；release EXE 已完成实际启动验证。

## 已知问题

- 开发环境使用 MinGW-w64 而非 MSVC；已通过 linker flag 解决 DLL 导出符号过多问题，后续 Windows 原生 API 兼容性需持续验证。
- FFmpeg / ffprobe 未打包，当前为可选外部依赖；未检测到时跳过媒体探测与视频缩略图。
- GNU 构建命令需要将 `C:\msys64\mingw64\bin` 加入当前 shell 的 `PATH`，以提供 `gcc` 与 `windres`。
- Windows 发布图标已替换为由版本控制 SVG 源生成的多分辨率 ICO；应用商店品牌资源与交互式安装视觉检查仍待完成。
- 真实用户媒体扫描与系统文件选择器交互仍待验证；隔离桌面 E2E 已覆盖扫描、暂停/恢复、崩溃恢复、无 API Key 关键词、参考图相似检索与 Entity 正/负颜色参考流程。

## 下一轮任务

1. 在 DaVinci Resolve、Premiere Pro、Final Cut Pro 中完成导入验证，并根据结果调整交换格式。
2. 用真实用户视频验证关键帧、短预览、黑帧/模糊检测和不同编码组合。
3. 在干净 Windows 环境验证交互式安装、卸载、升级和 MSI；不隐式安装浏览器或运行时。
4. 在 DaVinci Resolve、Premiere Pro、Final Cut Pro 中使用真实媒体完成导入验证，并根据结果调整交换格式。
